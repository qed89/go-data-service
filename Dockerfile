# Используем официальный образ Go с нужной версией
FROM golang:1.24.1

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем исходный код
COPY . .

# Собираем приложение
RUN go build -o go-data-service .

# Открываем порт микросервиса
EXPOSE 8081

# Команда для запуска микросервиса
CMD ["./go-data-service"]